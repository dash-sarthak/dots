### Variables
set $mod Mod4
set $left h
set $down j
set $up k
set $right l
set $term kitty
# set $menu j4-dmenu-desktop --dmenu="bemenu -i --border 2 -p 'Search:' --fn 'JetBrains Mono Nerd Font Mono Bold 10' --tb '#000000' --tf '#ffffff' --hb '#458588' --hf '#ffffff' --sb '#458588' --sf '#ffffff' --line-height 32 --ch 20 --nb '#000000' --ab '#000000' --fb '#000000' --hp 10 --bdr '#ffffff'"
set $menu rofi -show drun -display-drun ">>>"


## Startup applications
# On login
exec $HOME/.config/sway/scripts/battery-notifier.sh
exec mako

# On every reload
exec_always $HOME/scripts/autotiling
exec_always gsettings set org.gnome.desktop.interface gtk-theme 'Fluent-Dark'
exec_always gsettings set org.gnome.desktop.interface icon-theme 'Papirus-Dark'
exec_always /home/sarthak/.config/sway/scripts/clamshell.sh


### Output configuration
# Monitors
output eDP-1 resolution 2160x1440 position 0,0
output DP-1 resolution 2560x1440@75Hz position 2160,0

# Wallpaper
output "*" bg /home/sarthak/Pictures/Wallpapers/elementary/Morskie\ Oko.jpg fill


## Key bindings
# Basics:
    # Terminal
    bindsym $mod+Return exec $term

    # Kill current active window
    bindsym $mod+Shift+q kill

    # Launch app menu
    bindsym $mod+d exec $menu

    # Reload sway
    bindsym $mod+Shift+r reload

    floating_modifier $mod normal

    # Logout
    bindsym $mod+Shift+e exec wlogout -p layer-shell -b 3

    # Brightness
    bindsym --locked XF86MonBrightnessUp exec light -S "$(light -G | awk '{ print int(($1 + .72) * 1.4) }')" && $HOME/.config/sway/scripts/brightness-notify.sh
    bindsym --locked XF86MonBrightnessDown exec light -S "$(light -G | awk '{ print int($1 / 1.4) }')" && $HOME/.config/sway/scripts/brightness-notify.sh

    # Volume
    bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5% && $HOME/.config/sway/scripts/volume-notify.sh
    bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5% && $HOME/.config/sway/scripts/volume-notify.sh
    bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle && $HOME/.config/sway/scripts/mute.sh
    bindsym XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SOURCE@ toggle && $HOME/.config/sway/scripts/mute.sh

    # Screenshots:
        # Capture currently focussed monitor output to file and clipboard
        bindsym $mod+Shift+S exec $HOME/.config/sway/scripts/screenshot.sh output

        # Capture the specified screen area to file and clipboard
        bindsym $mod+Control+S exec /home/sarthak/.config/sway/scripts/screenshot.sh area


# Moving around:
    # Move your focus around
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    bindsym $mod+$right focus right

    # Or use $mod+[up|down|left|right]
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # Move the focused window with the same, but add Shift
    bindsym $mod+Shift+$left move left
    bindsym $mod+Shift+$down move down
    bindsym $mod+Shift+$up move up
    bindsym $mod+Shift+$right move right

    # Ditto, with arrow keys
    bindsym $mod+Shift+Left move left
    bindsym $mod+Shift+Down move down
    bindsym $mod+Shift+Up move up
    bindsym $mod+Shift+Right move right

# Workspaces:
    # Switch to workspace
    bindsym $mod+1 workspace number 1
    bindsym $mod+2 workspace number 2
    bindsym $mod+3 workspace number 3
    bindsym $mod+4 workspace number 4
    bindsym $mod+5 workspace number 5
    bindsym $mod+6 workspace number 6
    bindsym $mod+7 workspace number 7
    bindsym $mod+8 workspace number 8
    bindsym $mod+9 workspace number 9
    bindsym $mod+0 workspace number 10

    # Move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace number 1
    bindsym $mod+Shift+2 move container to workspace number 2
    bindsym $mod+Shift+3 move container to workspace number 3
    bindsym $mod+Shift+4 move container to workspace number 4
    bindsym $mod+Shift+5 move container to workspace number 5
    bindsym $mod+Shift+6 move container to workspace number 6
    bindsym $mod+Shift+7 move container to workspace number 7
    bindsym $mod+Shift+8 move container to workspace number 8
    bindsym $mod+Shift+9 move container to workspace number 9
    bindsym $mod+Shift+0 move container to workspace number 10

# Layout stuff:
    # Split the current object of your focus
    bindsym $mod+b splith
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    bindsym $mod+s layout stacking
    bindsym $mod+w layout tabbed
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

    # Move focus to the parent container
    bindsym $mod+a focus parent


# Scratchpad:
    # Move the currently focused window to the scratchpad
    bindsym $mod+Shift+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    bindsym $mod+minus scratchpad show

# Resizing containers:
mode "resize" {
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"


## Window rules
for_window [title="nmtui"] floating enable
for_window [title="Color Picker"] floating enable


## Decorations
    default_border pixel 2
    gaps inner 0

    #client.<class>         <border>    <background>   <text>      <indicator>    <child_border> 
    client.focused          "#B09E7E"   "#B09E7E"      "#000000"   "#B09E7E"      "#B09E7E"
    client.focused_inactive "#171820"   "#171820"      "#666666"   "#171820"      "#171820"
    client.unfocused        "#171820"   "#171820"      "#666666"   "#171820"      "#171820"

    # Status bar
    bar {
        swaybar_command waybar
    }
    
    # Configuration
    input type:touchpad {
        tap enabled
        natural_scroll enabled
    }


font pango:FiraCodeNerdFont Medium 10
